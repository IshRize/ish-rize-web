# Normalize line endings to LF on commit (prevents noisy CRLF diffs across platforms).
* text=auto eol=lf

# Treat binary assets as binary (never normalize).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
